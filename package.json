{
  "name": "arv-rest",
  "version": "0.0.1",
  "description": "A very easy rest (express like) api tool which allows you to create http api's with bun",
  "module": "src/index.ts",
  "type": "module",
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "release": "bun x changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/bun": "latest",
    "tsup": "8.0.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}